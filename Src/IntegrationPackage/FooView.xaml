<UserControl x:Class="IntegrationPackage.FooView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="183" d:DesignWidth="323">
	<StackPanel Orientation="Vertical">
		<TextBlock Text="Display name:" VerticalAlignment="Top" />
		<TextBox Text="{Binding DisplayName}" Width="100" VerticalAlignment="Top" />
        <TextBlock Text="Address:" VerticalAlignment="Top" />
        <TextBox Text="{Binding Address}" Width="100" VerticalAlignment="Top" />
        <TextBlock Text="Port:" VerticalAlignment="Top" />
        <TextBox Text="{Binding Port}" Width="100" VerticalAlignment="Top" />
    </StackPanel>
</UserControl>
